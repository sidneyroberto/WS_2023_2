paths:
  /tasks:
    post:
      summary: Salva uma nova tarefa
      tags: [tasks]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#components/schemas/Task"